# SalesScout Demo (Simple)

Cinematic iMessage-style product simulation for SalesScout. Single screen, 60-second flow. Hero/LinkedIn ad version.

## Edit

All copy and timing in the `CONFIG` object at the top of `index.html`.

## Deploy

```bash
cd ~/salesscout-demo-simple && vercel --prod
```
